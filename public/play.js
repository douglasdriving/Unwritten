import { getScenario, addAction, addScenario, monitorScenario } from "/dbHandler.js?v=0.1";

document.getElementById('playButton').onclick = () => {
    //addAction('example', 'Celebrate Christmas');
    //addScenario('A bomb fell from the sky', 'example');
    //monitorScenario('example', onUpdate);
}